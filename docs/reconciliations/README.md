# Replication Reconciliations

When independent replication is used, place reconciliation artifacts here:

```text
docs/reconciliations/NN-<focus>-replication-reconciliation.md
```

Preserve original independent reports under `docs/reports/`. Reconciliation must
not erase dissent. Contract:
`program/contracts/replication-reconciliation.md`.
